# Triton Learning

本项目用于完成 `问题1：Triton矩阵乘算子融合`，并且严格按 `问题1.md` 的三个阶段推进：

1. Step 1：用 PyTorch 建立 baseline，测量 `X@A`、`Y@B`、`X@C` 和最终加法的总时间。
2. Step 2：基于 Triton 官方 matmul 教程实现主干 GEMM，并与 PyTorch/cuBLAS 对比。
3. Step 3：当前新增一组“算子1 + 算子3”融合实验：融合阶段只输出 `Y=X@A` 与 `W=X@C`，后续仍单独执行 `Z=Y@B` 和 `O=W+Z`，并统一对比原版 PyTorch full 与 Triton 串行 full。

## 当前目录结构

```text
src/triton_learning/
├── benchmark_utils.py          # CUDA 计时、CSV 输出等公共逻辑
├── problem_spec.py             # 问题1统一形状配置
├── reference_ops.py            # PyTorch 参考实现
├── bench_step1_baseline.py     # Step 1 baseline 入口
├── bench_step2_triton_matmul.py# Step 2 benchmark 入口
├── bench_step3_fused_expand.py # Step 3 benchmark 入口
└── kernels/
    ├── matmul.py               # Triton 教程风格主干 GEMM
    └── fused_matmul_expand.py  # 单 LoRA 融合 kernel

src/fusion/
├── common.py                   # 三个新方案共用的 benchmark 胶水逻辑
├── scheme1_spatial_sharing.py  # 方案1：两个 Triton kernel + two streams
├── scheme2_horizontal_fusion.py# 方案2：保留 static_pid 与 grouped_persistent 两个单 kernel 口径
├── scheme3_column_concat.py    # 方案3：列拼接 GEMM
├── bench_scheme1_spatial_sharing.py
├── bench_scheme2_horizontal_fusion.py
└── bench_scheme3_column_concat.py
```

## 实现细节

### Step 1

- 目标形状按 `问题1.md` 当前要求固定为 `M=64, H=4096, N=28672, r=8`，对应 gateup_proj baseline。
- 参考实现统一放在 `reference_ops.py`，避免 Step 2/3 各自维护一份公式。
- `bench_step1_baseline.py` 同时输出：
  - 每个算子的单独耗时
  - 分项相加耗时
  - 完整串行流程 `O = X@C + (X@A)@B` 的总时间

### Step 2

- 主干 GEMM 的 Triton kernel 位于 `src/triton_learning/kernels/matmul.py`。
- kernel 结构遵循 `docs/Matmul.md` 中已经整理过的几个关键点：
  - tile 化分块
  - grouped ordering
  - fp32 accumulator
  - autotune 搜索不同 block 配置
- `bench_step2_triton_matmul.py` 会先验证 Triton 与 PyTorch 输出一致，再比较耗时与 TFLOPS。

参考学习资料：
- `问题1.md` 中的 Triton 官方 matmul 教程链接
- `docs/Matmul.md`

### Step 3

- 旧版本 `src/triton_learning/kernels/fused_matmul_expand.py` 保留为“算子2 + 算子3”融合尝试。
- 新版本实验放在 `src/fusion/`，目标只融合 `Y=X@A` 和 `W=X@C`，并分别保留三个方案的 benchmark。
- 方案1：`scheme1_spatial_sharing.py` 使用两个 Triton kernel 分别计算 `Y` 和 `W`，Python 侧用 `torch.cuda.Stream` 近似观察并发执行效果。
- 方案2：`scheme2_horizontal_fusion.py` 当前同时保留两个单 kernel 口径：
  - `static_pid`：把 `pid < num_tiles_1` 分给 `Y` tile，其余 pid 计算 `W` tile
  - `grouped_persistent`：前若干 worker 先做 `Y=X@A`，完成后回流到自己预留的 main chunk 条带，继续处理 `W=X@C`
- 方案3：`scheme3_column_concat.py` 使用 `[X@A, X@C] = X@[A,C]`，当前保留 `physical_precat`、`logical_no_pad`、`logical_rpad=128`、`logical_c_first_no_pad` 四个口径。
- 三个 benchmark 都额外记录两组 full baseline：
  - 原版 `PyTorch/cuBLAS full`
  - `Triton` 串行执行 `Y=X@A`、`W=X@C` 后再接后续算子的 full

参考学习资料：
- `docs/fusion.md`
- `docs/Punicaú║Multi-Tenant LoRA Serving.md`

## 复现方法

推荐的一条命令复现方式：

```bash
cd /home/starrys/triton_learning
bash scripts/reproduce_all_fusion_benchmarks.sh
```

如果只想准备环境但暂时不跑 benchmark：

```bash
bash scripts/activate_local_venv.sh
```

单独执行 Step 1：

```bash
bash scripts/run_step1_baseline.sh
```

单独执行 Step 2：

```bash
bash scripts/run_step2_triton_matmul.sh
```

单独执行旧 Step 3：

```bash
bash scripts/run_step3_fused_expand.sh
```

单独执行新的三个“算子1 + 算子3”融合 benchmark：

```bash
bash scripts/run_scheme1_spatial_sharing.sh
bash scripts/run_scheme2_horizontal_fusion.sh
bash scripts/run_scheme3_column_concat.sh
SCHEME2_VARIANT=grouped_persistent SCHEME2_NUM_DOWN_WORKERS=4 SCHEME2_CHUNK_SIZE=4 bash scripts/run_scheme2_horizontal_fusion.sh
```

如果要在 A100 上看老师关心的前两层证据（grid 级并行度、SM 级 occupancy），推荐直接使用仓库内置的 Nsight profiling 脚本。它们会调用 benchmark 的 `--profile-only` 模式，并自动打 NVTX 标记：

```bash
bash scripts/profile_suite.sh
bash scripts/profile_suite.sh baseline
bash scripts/profile_suite.sh scheme2
bash scripts/profile_suite.sh scheme2_persistent
TOOL=nsys bash scripts/profile_suite.sh
TOOL=ncu VARIANT=physical_precat bash scripts/profile_suite.sh scheme3
```

说明：

- `profile_suite.sh` 是 profiling 的唯一入口：
  - `baseline`：只看 Triton 串行 baseline 中的 `X@A` 与 `X@C`
  - `scheme1`：只看方案1的 concurrent pair
  - `scheme2`：只看方案2 `static_pid` 的 horizontal fused pair
  - `scheme2_persistent`：只看方案2 `grouped_persistent` 的 persistent pair
  - `scheme3`：只看方案3某一个变体的 pair，默认 `physical_precat`
- `TOOL=all` 时会同时跑 `nsys` 和 `ncu`；也可以切成 `TOOL=nsys` 或 `TOOL=ncu`。
- `profile_suite.sh` 跑 `nsys` 时会：
  - 只抓外层 NVTX `steady_state_capture` 区间，避免把 warmup / autotune 混进正式报告
  - 生成 `.nsys-rep`、`_profile.log`，以及 `cuda_gpu_kern_sum / cuda_gpu_trace / cuda_api_sum` 文本
- `profile_suite.sh` 跑 `ncu` 时会：
  - 先生成 `.ncu-rep`
  - 再从 `.ncu-rep` 单独导出 CSV，避免把 profiler 日志混进 CSV
  - 同时保留 `_collect.log` 与 `_export.log` 方便远端排错
- `nsys` 默认使用 `NSYS_PROFILE_WARMUP=1`、`NSYS_PROFILE_REPEAT=20`，这样在 GUI 里更容易直接看见稳定阶段的 kernel timeline 和两个子算子的边界。
- 为了提高远端执行成功率，`profile_suite.sh` 默认不强制打开 `nsys` GPU metrics；如果远端 A100 已开放权限，再显式加 `GPU_METRICS_DEVICE=all` 获取 `SM Active` 等硬件采样。
- 如果某些机器对 NVTX capture-range 不兼容，导致 `nsys` 没有落出报告，可临时加 `NSYS_USE_CAPTURE_RANGE=0`，退回到抓完整 profiling workload。
- `baseline` 的 Triton 串行报告会额外打出两个子区间：
  - `baseline/op1_xa_triton`
  - `baseline/op3_xc_triton`
  这样既能看整个串行 pair，也能单独观察 `X@A` 和 `X@C`。
- 某个 target 的 profiling 如果失败，脚本会继续尝试后续 target，并在最后统一汇总失败项，方便一次跑完后集中排查。
- 如果想改成完整采集，可加 `NCU_SET=full`：

```bash
NCU_SET=full TOOL=ncu bash scripts/profile_suite.sh scheme2
```

- 所有 profiling 脚本都支持：

```bash
M=64 H=4096 N=28672 R=8 DTYPE=fp16 TOOL=nsys NSYS_PROFILE_WARMUP=1 NSYS_PROFILE_REPEAT=20 bash scripts/profile_suite.sh scheme2
SCHEME2_NUM_DOWN_WORKERS=4 SCHEME2_CHUNK_SIZE=4 TOOL=nsys NSYS_PROFILE_WARMUP=1 NSYS_PROFILE_REPEAT=20 bash scripts/profile_suite.sh scheme2_persistent
GPU_METRICS_DEVICE=all TOOL=nsys NSYS_PROFILE_WARMUP=1 NSYS_PROFILE_REPEAT=20 bash scripts/profile_suite.sh scheme2
NSYS_USE_CAPTURE_RANGE=0 TOOL=nsys NSYS_PROFILE_WARMUP=1 NSYS_PROFILE_REPEAT=20 bash scripts/profile_suite.sh scheme2
```

- 如果想一条命令把四份 `nsys` / `ncu` 全部跑完：

```bash
VARIANT=physical_precat TOOL=all NSYS_PROFILE_WARMUP=1 NSYS_PROFILE_REPEAT=20 NCU_PROFILE_WARMUP=1 NCU_PROFILE_REPEAT=1 bash scripts/profile_suite.sh
```

如果需要自定义形状，可以在脚本前传环境变量，例如：

```bash
M=64 H=4096 N=28672 R=8 DTYPE=fp16 WARMUP=30 REPEAT=100 bash scripts/run_step3_fused_expand.sh
M=64 H=4096 N=28672 R=8 DTYPE=fp16 WARMUP=30 REPEAT=100 bash scripts/run_scheme2_horizontal_fusion.sh
WARMUP=5 REPEAT=20 bash scripts/reproduce_all_fusion_benchmarks.sh
```
