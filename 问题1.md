# 问题1：Triton矩阵乘算子融合

不包含LLM系统加速背景的版本：

### 问题描述

现有一个计算流程，包含三个串行执行的矩阵乘：

```
输入 X: [M, H]

算子1 (降维): Y = X @ A        其中 A: [H, r],  Y: [M, r]
算子2 (升维): Z = Y @ B        其中 B: [r, H'], Z: [M, H']
算子3 (主干): W = X @ C        其中 C: [H, H'], W: [M, H']

最终输出: O = W + Z    即 O: [M, H']
```

其中 `r` 远小于 `H` 和 `H'`（例如 `r=8`, `H=4096`, `H'=4096`, `M=64`）。

由于 `r` 很小，算子1和算子2的计算量非常小，无法充分利用GPU的计算资源。三个算子串行执行时，算子1和算子2各自都只能用到GPU的一小部分算力，但它们仍然各自占用一次kernel launch和一次完整的显存读写。

### 目标

将算子2（升维矩阵乘）与算子3（主干矩阵乘）融合成一个Triton kernel，使得：

- 两部分计算在同一个kernel内并行执行，提升GPU利用率
- 省去算子2的中间结果写回HBM再被算子3读取的开销
- 融合后的总执行时间 < 原来三个算子串行执行的时间

`本质上，就是把算子2和算子3融合成一个算子。可以自己设置几个参数，先用Triton生成三个算子，完成算子2和算子3的正确融合，再往下做`

### 实现步骤

**Step 1：确定目标矩阵维度并Profile**

以Llama3-8B模型、batch size=64为参考场景，确定各矩阵的具体维度。仅仅关注以下线性层的M/N/K：`gateup_proj`。降维秩 `r=8`。

使用PyTorch profiler或Nsight Systems分别测量三个独立矩阵乘的执行时间，作为性能基线。

**Step 2：实现主干矩阵乘的Triton版本**

参考Triton官方矩阵乘教程，实现一个高效的Triton GEMM kernel。

- 代码来源：`https://github.com/triton-lang/triton/blob/main/python/tutorials/03-matrix-multiplication.py`
- 官方文档：`https://triton-lang.org/main/getting-started/tutorials/03-matrix-multiplication.html`
- 该实现包含分块（tiling）、L2 cache优化、自动调参（autotune），在很多shape下可达cuBLAS 80-90%的性能
- 需要展示该实现在`gateup_proj` 下的性能相比于cublas的性能差

**Step 3：融合升维矩阵乘与主干矩阵乘**

从SGLang中获取升维矩阵乘的Triton实现（Punica SGMV expand kernel），将其与Step 2的主干GEMM融合成一个kernel。

- 升维算子代码来源：SGLang仓库中的Punica Triton kernel
    - 仓库地址：`https://github.com/sgl-project/sglang`
    - Triton LoRA后端路径：`python/sglang/srt/lora/backend/` 目录下的triton backend实现
    - Punica升维（expand）kernel路径：`python/sglang/srt/lora/triton_ops/`目录，关注 `sgmv_expand` 和 `chunked_sgmv_expand` 相关文件
- 融合思路：在主干GEMM kernel的每个thread block完成对应tile的计算后，将升维矩阵乘对应tile的结果直接累加到输出上，避免中间结果经过HBM

验证：融合kernel的数值结果与三个独立算子串行执行的结果一致（误差在fp16精度范围内）。

**Step 4（Bonus）：嵌入回SGLang并端到端验证**

将融合kernel集成回SGLang的模型执行路径，替换原有的分离执行方式，用实际模型推理验证端到端性能提升。做出来证明你是顶尖的programmer/researcher。

`bonus，做出来证明你是顶尖的programmer/researcher，一起做科研吧，去拿天才少年！！`

### 参考资源

| 资源 | 地址 | 用途 |
| --- | --- | --- |
| Triton官方matmul教程 | `github.com/triton-lang/triton` → `python/tutorials/03-matrix-multiplication.py` | Step 2的GEMM起点 |
| Triton文档 | `triton-lang.org/main/getting-started/tutorials/03-matrix-multiplication.html` | 理解tiling和autotune |
| SGLang仓库 | `github.com/sgl-project/sglang` | Step 3的Punica kernel来源 |
| SGLang LoRA Triton算子 | `python/sglang/srt/lora/triton_ops/` 目录 | sgmv_expand / chunked_sgmv_expand |
| SGLang LoRA后端逻辑 | `python/sglang/srt/lora/backend/` 目录 | 理解LoRA后端如何调用kernel |
| Punica论文 | arXiv:2310.18547 | 理解SGMV算子的设计原理 |

---

包含LLM系统加速背景的版本：

问题1背景：

- LoRA是现阶段LLM重要的微调技术
- 现在vLLM/SGLang在支持多个LoRA的服务的时候，默认把LoRA部分从base model的执行中拆分出来；单独执行
- 由于LoRA的计算是低秩矩阵，所有LoRA基本上吃不满GPU；所以现在vLLM和SGLang支持两种LoRA实现（一种是Triton默认实现，一种是Punica，也是基于Triton来实现的）
- LoRA计算分两步，一个是降维矩阵乘，一个是升维矩阵乘
- LoRA可以附属在LLM的几个GEMM position（base layer）

问题1内容：

- 将升维矩阵乘算子，与base layer的GEMM算子，融合成一个算子

实现优势：

- 原本三个算子都是串行执行，LoRA的两个算子无法利用满GPU
- 融合之后，两个算子同时执行，融合算子能利用满GPU；
- 融合算子的执行时间比算子串行执行少，性能提升

具体实现思路：

1. profile出Llama3-8B在bs=64的情况下的base layer的GEMM维度，LoRA rank设为8
    1. 关注q_proj/k_proj/v_proj/o_proj/gate_proj/up_proj/down_proj这几个线性层的M/N/K维度
    2. gate_proj和up_proj在SGLang/vLLM中是合并成一个MergedColumnParallelLinear的
2. 为base layer找到一个高效的GEMM实现Triton版本
    1. Triton官方repo里有一个经典的矩阵乘教程，实现了分块（tiling）+ L2 cache优化的GEMM，在很多shape下能达到cuBLAS 80-90%的性能。
    2. 地址：`github.com/triton-lang/triton` → `python/tutorials/03-matrix-multiplication.py`
    3. 可以先用这个做实验
3. 使用SGLang中的Punica Triton LoRA kernel，将升维矩阵乘算子，与base layer的矩阵算子，融合成一个算子，提升GPU利用率
4. 嵌入回SGLang，并验证性能提升（bonus，做出来证明你是顶尖的programmer/researcher）

你的直觉基本对：**如果只是把 `A` 和 `C` 在逻辑上看成拼接矩阵 `[A, C]`，并在一个 Triton kernel 里分别从 `A`、`C` 读，理论计算量和必要读写量不会增加太多**。但是要区分两种情况：

```text
情况 1：真的提前构造 AC = [A, C]
    会多一次写 AC 的开销，除非 AC 是预先离线准备好的。

情况 2：不真的构造 AC，只是在 Triton kernel 里按列号分别读 A 或 C
    不需要额外写 AC，但 kernel 里 load 逻辑更复杂，边界 tile 会有分支/mask。
```

你现在提出的拼接方法，确实比单纯两个 kernel 空分复用更像“自然地占满更多 SM”。

---

## 1. 你的拼接融合方法是什么？

原来有：

```text
算子1：Y = X @ A      A: [H, r],   Y: [M, r]
算子3：W = X @ C      C: [H, H'],  W: [M, H']
```

因为二者左矩阵都是 `X`，所以可以写成：

```text
[Y, W] = X @ [A, C]
```

其中：

```text
[A, C]: [H, r + H']
[Y, W]: [M, r + H']
```

这个方法的本质是：**把两个共享输入 `X` 的 GEMM 合并成一个更宽的 GEMM。**

---

## 2. 这样会不会增加 IO？

### 从理论必要 IO 看，不会明显增加

原来两个 kernel 分别做：

```text
kernel1: 读 X，读 A，写 Y
kernel3: 读 X，读 C，写 W
```

也就是：

```text
读 X 两次
读 A 一次
读 C 一次
写 Y 一次
写 W 一次
```

拼接后做：

```text
fused: 读 X，读 A/C，写 [Y,W]
```

理论上变成：

```text
读 X 一次或更少重复
读 A 一次
读 C 一次
写 Y/W 一次
```

所以从数据流上看，它甚至可能**减少对 X 的重复读取**，因为算子1和算子3共享 `X`。

不过要注意，真实 GPU 上一个大 GEMM 不是全局只读一次 `X`。每个 output tile 还是会按照 tiling 读取对应的 `X` block。能不能真的减少 X 的 global memory traffic，要看 Triton kernel 的 tile 组织方式。

但是至少它不会天然比两个 kernel 多很多 IO。

---

## 3. 什么时候会增加 IO？

主要是这几种情况。

### 情况 A：运行时真的 `torch.cat([A, C])`

如果你每次 forward 都做：

```python
AC = torch.cat([A, C], dim=1)
T = X @ AC
```

那么 `torch.cat` 本身会产生额外开销：

```text
读 A
读 C
写 AC
```

这会增加 IO。

如果 `A` 和 `C` 是固定权重，比较好的方式是：

```text
提前离线构造 AC
或者初始化时构造一次 AC
```

这样 forward 时就没有额外 cat 开销。

但如果 `A` 是每个 request/adapter 动态变化的 LoRA 权重，而 `C` 是主干权重，那么每次都物理拼接可能不划算。

---

### 情况 B：不物理拼接，而是在 Triton 里逻辑拼接

比如 fused kernel 的列号 `n`：

```text
n < r      → 读 A[:, n]
n >= r     → 读 C[:, n-r]
```

这样不需要真的构造 `AC`。

但问题是：边界 tile 会比较麻烦。

假设：

```text
r = 8
BLOCK_N = 64
```

第一个 N 方向 tile 覆盖：

```text
columns 0 ~ 63
```

其中：

```text
columns 0 ~ 7   属于 A
columns 8 ~ 63  属于 C
```

也就是说同一个 tile 里一部分列读 `A`，另一部分列读 `C`。

这会带来：

```text
1. load 地址计算更复杂
2. mask 更复杂
3. 可能影响内存 coalescing
4. Triton 代码更难写
```

但这不一定是大的 IO 开销，更多是**实现复杂度和访存连续性问题**。

---

### 情况 C：A 和 C 的 dtype/layout 不同

这是很关键的现实问题。

如果：

```text
C 是 int4/int8 量化权重
A 是 fp16/bf16 LoRA 权重
```

那你不能简单把它们当成一个统一矩阵 `[A, C]` 来做同一个 `tl.dot`。

因为主干 GEMM 和 LoRA down GEMM 的：

```text
dtype
scale
layout
dequantization 逻辑
```

可能不一样。

这时“数学上拼接”仍然成立，但“工程上作为一个普通 GEMM”不一定成立。

---

## 4. 和两个 kernel 空分复用相比，哪个更好？

我倾向于说：

> **如果 A 和 C 的 dtype/layout 兼容，并且可以避免运行时物理 cat 开销，那么 `[Y,W] = X @ [A,C]` 通常比两个 kernel 空分复用更自然、更稳。**

原因是它解决的是根本问题。

---

## 5. 为什么拼接方法更自然地占满更多 SM？

算子1单独跑时：

```text
Y = X @ A
Y: [M, r]
```

tile 数是：

```text
num_tiles_1 = ceil(M / BLOCK_M) * ceil(r / BLOCK_N)
```

因为 `r` 很小：

```text
ceil(r / BLOCK_N) = 1
```

所以：

```text
num_tiles_1 ≈ ceil(M / BLOCK_M)
```

它天然 tile 少，SM 利用率低。

但拼接后：

```text
T = X @ [A, C]
T: [M, r + H']
```

tile 数变成：

```text
num_tiles_fused = ceil(M / BLOCK_M) * ceil((r + H') / BLOCK_N)
```

因为 `H'` 很大，所以：

```text
num_tiles_fused ≈ ceil(M / BLOCK_M) * ceil(H' / BLOCK_N)
```

这几乎就是主干 GEMM 的规模。

也就是说，算子1的小输出列 `r` 被挂到了主干 GEMM 的大输出维度旁边，不再单独形成一个很窄的小 kernel。

所以它确实能更自然地占满更多 SM。

---

## 6. 两个 kernel 空分复用的问题在哪里？

两个 kernel 空分是：

```text
kernel1: Y = X @ A
kernel3: W = X @ C
```

让它们并发运行：

```text
一部分 SM 跑 kernel1
一部分 SM 跑 kernel3
```

这个思路理论上也合理，但有几个问题。

### 问题一：算子1太小，可能连分到的 SM 都填不满

如果算子1只有很少 tile，比如：

```text
num_tiles_1 = 4 或 8
```

那即使你给它 20 个 SM，它也可能只用到 4 或 8 个 SM。

所以空分复用并不自动解决算子1 tile 少的问题。

它只是说：

```text
算子1和算子3同时跑
```

但如果算子1本身任务数太少，它仍然没有足够 block 可以铺满分到的 SM。

---

### 问题二：两个 kernel 并发调度不一定可控

普通 CUDA stream 并发不能保证：

```text
kernel1 固定用 SM0-SM19
kernel3 固定用 SM20-SM79
```

除非使用 Green Context、MPS、MIG 或更底层的机制。

所以“两个 kernel 空分复用”要做得严格，其实实现复杂度更高。

---

### 问题三：两个 kernel 都要读 X

算子1和算子3都读 `X`。

两个 kernel 分开跑：

```text
kernel1 读 X
kernel3 也读 X
```

拼接 GEMM 至少在算法表达上把它们变成一个 GEMM：

```text
X @ [A, C]
```

有机会让 `X` 的 tile 在同一个 program 内复用，虽然具体能复用多少要看实现。

---

### 问题四：kernel1 太小，launch overhead 占比高

算子1因为 `r` 小，计算量也小。

单独作为一个 kernel 发射时，可能出现：

```text
计算时间很短
launch overhead / 调度 overhead 占比明显
```

拼接方案直接消除了算子1独立 kernel。

---

## 7. 但是拼接方法也不是无条件更好

它更好的前提是：

```text
1. A 和 C 的数据类型、布局、计算路径兼容；
2. 不需要每次 forward 都昂贵地物理拼接 AC；
3. 拼接后 GEMM 的 BLOCK_N / tile 形状不会显著破坏主干 GEMM 性能；
4. r 相对 H' 很小，额外列不会显著增加主干 GEMM 的调度复杂度；
5. 后续可以方便地取出 Y 和 W。
```

尤其是第 3 点要实测。

因为主干 GEMM `X @ C` 可能原本已经有一个很优化的 shape。你把前面插入 `r` 列后：

```text
N = H' 变成 N = r + H'
```

通常影响不大，因为 `r << H'`。

但如果 `r` 导致对齐变化，比如原本 `H'` 是 4096，很整齐；现在变成 4104，可能影响 tile 对齐或 store 对齐。

可以通过 padding 解决：

```text
[A_pad, C]
```

把 `r` padding 到 16/32/64 的倍数。

例如：

```text
r = 8
pad_r = 16
AC = [A_pad, C]
```

这样更适合 Tensor Core/tile 对齐。

---

## 8. 哪个更符合老师的问题？

老师的问题是：

> 再想清楚算子1和算子3怎么在 GPU 上一起运行，他们的效果最好。

你这个拼接方案其实是一个非常好的回答，因为它说清楚了：

```text
算子1和算子3共享左输入 X；
它们写不同输出 Y 和 W；
所以最自然的方式不是让两个 kernel 抢 SM，
而是把右矩阵 A 和 C 在列方向拼接，
将两个 GEMM 变成一个更宽的 GEMM。
```

这比“两个 kernel 空分”更有数学结构。

你可以这样对比：

```text
两个 kernel 空分：
    调度层面的并行。
    依赖 GPU 调度/SM partition。
    算子1仍然是 skinny GEMM，tile 少的问题仍然存在。

列拼接融合：
    代数层面的融合。
    把 skinny GEMM 合进主干 GEMM。
    直接增加输出 N 维宽度，使 tile 数接近主干 GEMM。
    更自然地利用 SM。
```

所以如果你的工程条件允许，**列拼接融合更优雅，也更可能稳定提升**。

---

## 9. 更严谨的结论

可以这样判断：

### 如果 `A` 和 `C` 可以被当成同 dtype、同 layout 的矩阵处理

优先考虑：

```text
[Y, W] = X @ [A, C]
```

这是更自然的融合。

它的优势是：

```text
1. 消除算子1独立 kernel；
2. 解决算子1输出窄导致的 tile 数少；
3. 有机会复用 X tile；
4. 不需要依赖不确定的 SM 调度；
5. 不存在 Y/W 写冲突。
```

---

### 如果 `A` 和 `C` 不能直接拼

比如：

```text
C 是量化主干权重
A 是 fp16 LoRA 权重
```

或者主干 GEMM 使用了特殊 kernel，那么列拼接普通 GEMM 不好做。

这时再考虑：

```text
1. 逻辑拼接：同一个 Triton kernel 中不同列区域用不同 load/dequant 逻辑；
2. 两个 kernel 并发空分；
3. pid 分区的横向融合；
4. 继续保持分开 kernel，但优化算子1的 skinny GEMM。
```

---

## 10. 可以发给老师的版本

老师，我现在觉得算子1和算子3一起运行最自然的方式可能不是简单做两个 kernel 的空分，而是利用它们共享左矩阵 `X` 的结构：

算子1：`Y = X @ A`，`A: [H, r]`
算子3：`W = X @ C`，`C: [H, H']`

因为矩阵乘法对列拼接满足：

`[X @ A, X @ C] = X @ [A, C]`

所以可以把 `A` 和 `C` 在列方向拼接成 `[A, C]`，然后一次 GEMM 得到 `[Y, W]`。这样算子1不再是单独的 skinny GEMM，而是被合并到主干 GEMM 的大输出维度里。

我理解这个方法相比两个 kernel 空分可能更自然：两个 kernel 空分只是调度层面让算子1和算子3同时跑，但算子1本身 `[M, r]` 输出很窄、tile 数少的问题没有消失；而列拼接后输出变成 `[M, r + H']`，tile 数接近主干 GEMM，更容易自然占满 SM，也不依赖 block 到 SM 的调度顺序。

不过这个方案的前提是 `A` 和 `C` 的 dtype/layout/计算路径兼容。如果物理上每次都 `cat(A, C)`，会有额外 IO；更好的方式是预先拼接，或者在 Triton kernel 里逻辑拼接，根据列号分别从 `A` 和 `C` 读取。逻辑拼接不会额外写出 `[A,C]`，但边界 tile 的 load/store 会复杂一些。

---

## 11. 最终回答你的问题

> 不连续矩阵拼接以及读写，是否会增大 IO 开销？

**如果你不真的生成 `[A,C]`，只是逻辑拼接，一般不会显著增加必要 IO；反而可能减少 X 的重复读取。**

但它会增加：

```text
地址计算复杂度
mask 复杂度
边界 tile 处理复杂度
可能的访存不连续问题
```

如果你每次都物理 `cat(A,C)`，那就会增加 IO。

---

> 和两个 kernel 空分复用比较，哪个更好？

在 `A` 和 `C` 兼容的前提下，**列拼接融合通常更自然、更稳，也更符合“算子1和算子3一起运行效果最好”的思路**。

因为它不是靠调度层面硬并发，而是把两个 GEMM 变成一个更大的 GEMM：

```text
[Y, W] = X @ [A, C]
```

---

> 这个能不能更自然地占满更多 SM？

能。

因为算子1原来输出 `[M, r]`，tile 数少；拼接后输出 `[M, r + H']`，tile 数接近主干 GEMM，自然拥有更多可调度 program/block。

所以这句话可以作为你的核心结论：

> **两个 kernel 空分复用是在调度层面补救算子1的低利用率，而列拼接融合是在矩阵形状层面消除算子1的 skinny GEMM 问题。若 dtype/layout 允许，列拼接融合更自然。**
